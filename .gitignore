# build artifacts
build/
build-*/
cmake-build-*/
out/

# editor / IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# binaries
*.o
*.obj
*.exe
*.dll
*.so
*.dylib
*.a
*.lib
*.pdb
*.ilk
# but keep Wavefront model files under data/
!data/**/*.obj

# CMake leftovers
CMakeCache.txt
CMakeFiles/
CMakeScripts/
cmake_install.cmake
install_manifest.txt
Makefile
compile_commands.json

# fetched deps (if anyone configures outside build/)
_deps/

# local agent config
.claude/

# runtime-written
settings.cfg

# raw asset packs (commit only the converted files under data/)
/Animated FPS Guns*

# screenshots / viewmodel captures dropped in the repo root (textures
# under data/ are still tracked)
/*.png
